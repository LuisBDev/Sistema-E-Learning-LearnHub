let express,cors,readdirSync,mongoose;_aa2‍.w("express",[["default",["express"],function(v){express=v}]]);_aa2‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_aa2‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_aa2‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);




const morgan = require("morgan");
require("dotenv").config();

//creando express app
const app = express();


// db
mongoose.connect(process.env.DATABASE, {
    userNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreateIndex: true,
  })
  .then(() => console.log("**DB CONNECTED**"))
  .catch((err) => _aa2‍.g.console.log("DB CONNECTION ERR => ", err));


//aplicando middlewares

app.use(cors());
app.use(express.json());
app.use(morgan('dev'));

//importando rutas
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)));

//definiendo puerto
const port = process.env.PORT || 8000;

//escuchando el puerto
app.listen(port, () => {
    console.log(`Server corriendo en puerto:  ${port}`);
    }
);
